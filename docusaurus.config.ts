import { Config } from '@docusaurus/types';

const config: Config = {
  title: 'Soroban ZK Toolkit',
  url: 'https://example.com',
  baseUrl: '/',
  projectName: 'soroban-zk-docs',
};

export default config;
